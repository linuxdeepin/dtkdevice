@PACKAGE_INIT@

set_and_check(DtkDevice_INCLUDE_DIRS "@PACKAGE_INCLUDE_INSTALL_DIR@")
set_and_check(DtkDevice_LIBRARY_DIRS "@PACKAGE_LIBRARY_INSTALL_DIR@")

include(CMakeFindDependencyMacro)
find_dependency(Core dbus)
find_library(DtkDevice_LIBRARIES dtkdevice ${DtkDevice_LIBRARY_DIRS})
set(DtkDevice_LIBRARIES ${DtkDevice_LIBRARIES} ${DtkCore_LIBRARIES} )

include_directories("${DtkDevice_INCLUDE_DIRS}")

check_required_components(DtkDevice)

# Keep deprecated variables for compatibility
set(DTKDEVICE_INCLUDE_DIR ${DtkDevice_INCLUDE_DIRS})
